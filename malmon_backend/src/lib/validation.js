/**
 * Checks whether a string is a string.
 * @param {s} string to be validated.
 * @returns {boolean} true if string is string, otherwise false.
 */
export function isString(s) {
  return typeof s === 'string';
}
